# Admin URLs are application routes, not a browsable static directory.
Options -Indexes
DirectoryIndex disabled
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^ ../index.php [L]
</IfModule>
