# Admin / Site Settings audit — 2026-09-23 (current session)

این بخش نتیجه بررسی و تغییرات این جلسه روی مبنای `57e0f7c` است. مطالب پایین با عنوان **Historical audit** از جلسه قبلی حفظ شده‌اند؛ ادعاهای آن درباره نبود Installer/Users/لوگو وضعیت فعلی نیستند.

## یافته‌های واقعی و اصلاحات

- جدول یا سرویس Settings وجود نداشت. `site_settings` با primary key یکتا و migration همسان schema اضافه شد؛ سرویس مرکزی یک بار در هر درخواست تنظیمات را می‌خواند.
- `views/layouts/main.php`, `views/layouts/admin.php`, `pages/home.php`, `pages/public_detail.php` و JSON-LD در `app/Helpers/functions.php` هویت ثابت داشتند. اکنون از تنظیمات و fallback استفاده می‌کنند. Installer قبل از نصب عمداً از فایل ثابت استفاده می‌کند.
- `/admin/settings` اکنون فرم فارسی RTL نام، توضیح، لوگو، favicon و OG دارد؛ پیش‌نمایش فایل فعلی/انتخابی، وضعیت فایل، ذخیره و بازگردانی جداگانه هر تصویر دارد.
- Settings و Users به guard admin موجود متصل‌اند؛ نقش/فعال‌بودن حساب از DB نیز کنترل می‌شود تا session مدیر حذف‌شده/تنزل‌یافته این مجوزها را حفظ نکند. Editor همچنان طبق سیاست موجود مدیریت محتوایی دارد، نه Settings/Users.
- تغییرات تنظیمات و ارتقا POST-only و دارای CSRF موجود هستند. مسیرهای ارتقا auth/CSRF جدید یا Installer موازی ایجاد نمی‌کنند.
- فایل تصویر با `is_uploaded_file`, MIME واقعی finfo، پسوند همخوان، اندازه واقعی دیسک و محدودیت ابعاد بررسی می‌شود. SVG/HTML/PHP پذیرفته نمی‌شود؛ نام اصلی در مسیر ذخیره استفاده نمی‌شود. نام تصادفی ۱۹۲بیتی و رزرو انحصاری از overwrite جلوگیری می‌کند.
- `uploads/site/.htaccess` فقط نام‌های تولیدی raster را مجاز می‌داند و handler را static می‌کند. سیاست والد اکنون پسوندهای عدددار PHP را نیز مسدود می‌کند. شاخه توسعه `php -S` دیگر هر فایل دلخواه uploads را به سرور واگذار نمی‌کند.
- ذخیره چند تنظیم تراکنشی است: خطا فایل‌های جدید را پاک می‌کند، حذف فایل قدیمی فقط بعد از commit انجام می‌شود. crash یا شکست unlink ممکن است orphan باقی بگذارد؛ پاک‌سازی اجباری cron اضافه نشده است.
- Installer قبلاً متن خام خطای MySQL را با regex ناکافی چاپ می‌کرد. اکنون فقط نام مرحله، SQLSTATE محدود و شماره عددی خطا چاپ می‌شود؛ نه متن exception/driver. نوشتن config پس از تأیید schema انجام می‌شود؛ بررسی requirements پیش از توابع نیازمند extension انجام می‌شود.
- مقادیر غیرمحرمانه Deployment در Installer پیش‌فرض شدند؛ رمز دیتابیس دریافت، گزارش یا commit نشده است.
- runner migration از Installer به helper مشترک منتقل شد؛ checksum فایل اجراشده و خوانا بودن فایل بررسی می‌شود. ارتقای صریح admin برای نصب قبلی، بدون حذف installed.lock یا بازنویسی مدیر، اضافه شد.
- در Content Registry، ویرایش کتاب/درس/پژوهش به اشتباه فرم عمومی چهار نوع محتوا را باز می‌کرد. اکنون به editor موجود همان نوع redirect می‌شود؛ ایجاد generic با فیلتر این انواع نیز اصلاح شد.
- آپلود رسانه معمولی JPEG را به دلیل تطابق فقط `jpg` رد می‌کرد؛ alias اصلاح شد و اندازه واقعی فایل جای اندازه ورودی ملاک شد. شمارنده مصرف رسانه اکنون تصویر جلد محتوا را نیز حساب می‌کند.
- robots مسیر نصب زیرپوشه را لحاظ نمی‌کرد؛ مسیرهای ممنوع اکنون prefix نصب دارند. canonical/sitemap از helperهای موجود باقی ماندند؛ OG تصویر اختصاصی مطلب را حفظ می‌کند و در نبود آن تنظیمات عمومی را به شکل absolute استفاده می‌کند.
- CSS روی design system موجود توسعه یافت؛ تصاویر با object-fit:contain و اندازه محدود، grid تنظیمات با minmax(0,1fr)، کنترل‌های بدون min-width اضافی و pagination قابل wrap. منو/فوکس/دراور موجود دوباره‌سازی نشد.

## دامنه بررسی کد مدیریت

Route table و تمام viewهای `pages/admin/`، layout مدیریت، helper مدیریت، guard نقش‌ها، CSRF/session، repositoryهای کاربر/محتوا/رسانه، schema و SQL migration، CSS/JS و سیاست Apache بررسی شدند. جدول دقیق routeها در انتهای `ROUTES.md` ثبت شده است:

- Dashboard، registry عمومی و میانبرهای news/articles/reports/events؛ فرم‌های generic create/edit؛ relation و publish/unpublish/archive/delete.
- Users list/new/edit/delete و حفظ محدودیت آخرین مدیر موجود.
- Media list/upload/delete؛ Topics list/create و edit/delete در همان فهرست. `GET /admin/topics/{id}/edit` صفحه مجزا نیست.
- Books/Lessons/Research registry/new/edit/alias و عملیات وضعیت/حذف.
- Settings GET/POST و upgrade POST جدید؛ هیچ عملیات mutating GET اضافه نشده است.

SQL جدید فقط statement ثابت و bind parameters دارد؛ مقادیر متن هنگام خروجی HTML escape می‌شوند و JSON-LD با JSON_HEX flags موجود تولید می‌شود. آپلود مسیر ورودی را به unlink/move نمی‌دهد؛ مسیرهای ذخیره‌شده نیز قبل از استفاده/حذف محدود می‌شوند. این **بازبینی کد** است، نه ادعای موفقیت penetration test روی سرور واقعی.

## تست‌ها و وضعیت صادقانه

| بررسی | نتیجه این جلسه |
|---|---|
| `git diff --check` | موفق |
| PHP grammar مستقل با tree-sitter | ۸۵ فایل بدون خطای grammar؛ ۱ هشدار parser در `tests/security/BasicsTest.php`، عیناً در baseline هم موجود. این جایگزین `php -l` نیست. تمام فایل‌های PHP تغییرکرده بدون خطای grammar بودند. |
| JavaScript parse با esprima | `assets/js/main.js` موفق؛ بدون Node/build step |
| CSS top-level parse با tinycss2 | موفق؛ اثبات صحت layout مرورگر نیست |
| مسیر literal assetهای PHP/CSS | ۳ مسیر literal CSS/JS/لوگوی Installer موجود؛ سه fallback تصویر نیز جدا بررسی شدند. مسیرهای dynamic/upload نیازمند تست runtime هستند. |
| SVG fallback | XML خوانا؛ لوگو و OG فاقد script/event-handler طبق اسکن ثابت؛ SVG آپلودی اصلاً مجاز نیست |
| schema/migration تطابق | تعریف جدول جدید یکسان است؛ بررسی متنی، نه اجرای MySQL |
| `bash tests/run_all.sh` | اجرا شد ولی به علت `php: command not found` ناموفق؛ suite سبز نیست |
| Native PHP syntax/unit/integration/security/route/HTTP/schema | اجرا نشد/مسدود: PHP موجود نیست |
| Apache `.htaccess` | harness به Apache دسترسی نداشت و skip کرد؛ pass محسوب نمی‌شود |
| نصب واقعی InfinityFree / MySQL | انجام نشده؛ اتصال production یا رمز درخواست نشد |
| multipart واقعی و جایگزینی فایل | تست اجرایی انجام نشده |
| مرورگر و عرض‌های 360/375/390/430/768/1024/1280+ | CSS بازبینی شد؛ تست دیداری/اندازه‌گیری overflow انجام نشده |

تلاش نصب PHP از apt (HTTP/HTTPS) و انتشار PHP 8.3 در GitHub به محدودیت اتصال شبکه برخورد کرد. ابزارهای Python فقط برای بررسی محلی در مسیر خارج مخزن نصب شدند؛ وابستگی production یا build step اضافه نشده است.

دو suite جدید: `SiteSettingsTest.php` و `BrandingSecurityTest.php` شامل **۱۵ تست** برای RBAC، CSRF تمام POSTهای ثبت‌شده Admin، session مدیر لغوشده، ذخیره و یکتایی، fallback، Header/Footer/favicon/OG، subdirectory، XSS، MIME/پسوند، اندازه، traversal، PHP/SVG/HTML، عدم قبول فایل محلی به‌جای HTTP upload و مسیرهای knowledge هستند. **این تست‌های PHP نوشته شده‌اند ولی اجرا نشده‌اند.** تست‌های قدیمیِ توقع نبود users/settings و فهرست schema/services با قابلیت واقعی همسو شدند؛ محدودیت‌های security حذف نشده‌اند.

## پذیرش نهایی / اقدامات باقی‌مانده

پیاده‌سازی در Git انجام شده است؛ تأیید نهایی production منوط به اجرای suite روی PHP 8.3+/PDO و MySQL، آپلود واقعی، تست Apache و چک‌لیست مرورگر در `BRANDING-AND-INSTALLER.md` است. هیچ نتیجه اجرا‌نشده‌ای PASS اعلام نمی‌شود. نصب/انتشار روی دامنه واقعی در این جلسه انجام نشده است.

---

# Historical audit (previous work; retained for history)

# Audit — 2026-09-23

Scope: full repository review for a plain Raw PHP, Persian RTL, Apache/MySQL shared-hosting deployment.

## Repository structure reviewed

- Front controller: `index.php`
- Route table: `router.php`
- Apache rules: `.htaccess`, `admin/.htaccess`, `uploads/.htaccess`
- Configuration: `config/config.php`, `config/database.php`, `config/local.example.php`
- Helpers/services: `app/Helpers/*`, `app/Services/*`, `app/Middleware/AuthGuards.php`
- Repositories: `app/Repositories/*`
- Public/admin views: `pages/*`, `pages/admin/*`, `views/layouts/*`, `views/partials/*`
- Assets: `assets/css/main.css`, `assets/js/main.js`
- Database: `database/schema.sql`, `database/migrations/*`, `database/seed.sql`
- Tests/docs/uploads rules: `tests/*`, `docs/*`, `uploads/.htaccess`

## Confirmed strengths from current code

- Pure PHP front controller; no framework, Node.js, Docker, Redis or external service dependency.
- PDO helper layer uses prepared statements and disables emulated prepares.
- Public content queries filter `status = 'published'` and `published_at <= now()` in repositories.
- Session setup enables strict mode, HTTP-only cookies, SameSite=Lax and regenerates ID after login.
- CSRF tokens exist for login/logout/admin mutations.
- Login rate limiting is database-backed in `login_attempts` and requires no external cache.
- Upload directory has an `.htaccess` that disables PHP/script execution and directory indexing.
- URL helper and request normalizer already support installations under subdirectories.
- Public routes for `/`, news, articles, reports, events, books, lessons, research, media, topics, search, login, admin and sitemap/robots are present.

## Issues found from actual files

### Installer / deployment

- No `install.php` exists, so a first-time InfinityFree user cannot create `config/local.php`, install schema, run migrations or create the first admin through the browser.
- No install lock exists; reinstall protection is absent.
- `schema_migrations` table is absent, so migration execution cannot be recorded.
- `uploads/media/` is used by upload code but has no tracked placeholder/unignore rule.

### Database alignment

- `events` and `reports` extension tables exist, but the generic admin content create/update route does not create/update those extension rows.
- `users` table exists, but the admin UI has no usable user management routes/views.
- `media` accepts image/audio/video/document, but the media admin route lists only images.
- Migration execution is documented but not integrated into runtime installation.

### Routing / subdirectory

- Public route coverage is mostly present. Admin route coverage is partial: `/admin/content` exists, but type-specific admin pages are only listing shortcuts and users are disabled in the menu.
- Search and 404 responses do not consistently emit `noindex` metadata.

### UI/UX and logo

- No uploaded logo file is present in the repository or workspace. The current header uses a text mark only.
- The CSS contains a duplicated `.hero h1 {` block, which can break following rules in some parsers.
- Mobile hamburger toggles the menu, but there is no overlay, scroll lock, close-on-link-click, or explicit outside close handling.
- Admin dashboard text still says some sections are not active even though routes exist.

### Admin

- Admin authorization is currently admin-only for all admin work. The requested role separation (`user`, `editor`, `admin`) is not fully reflected.
- Generic content form lacks cover media, file/media attachment selection and related-content controls.
- Generic content registry lacks publish/archive/delete actions.
- Topic admin is create/list only; update/delete/archive-style management is absent.
- User admin is absent.

### Security

- SQL is mostly parameterized; remaining risks are authorization/IDOR around admin routes because all are admin-only but editor role is not implemented.
- Upload route checks MIME with `finfo`, size and random names, but does not validate original extension explicitly and stores all files in one `uploads/media` path not tracked in the repo.
- Delete/archive flows for generic content are missing; adding them must include CSRF and server-side authorization.
- Installer must never display or commit database credentials.

### SEO/performance/accessibility

- Base title/meta/canonical/Open Graph/Twitter tags exist.
- JSON-LD support exists in the layout, but public route handlers do not populate Organization/Article/NewsArticle/Book/Event/LearningResource/ScholarlyArticle structured data.
- Sitemap and robots routes exist; robots disallows admin/login/search.
- Search should be noindexed by meta as well.
- Public markup is mostly semantic with skip link and focus styles, but logo/favicons and responsive image/OG logo assets are missing.

### Local test environment limitation

- This sandbox currently has no `php` binary (`php: command not found`), so PHP syntax/runtime tests cannot be executed here. Static inspection, shell checks and file-level validation are still possible; final runtime verification should be done on PHP 8.3+ or the target InfinityFree environment.
